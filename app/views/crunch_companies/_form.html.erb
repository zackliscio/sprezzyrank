<%= form_for(@crunch_company) do |f| %>
  <% if @crunch_company.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@crunch_company.errors.count, "error") %> prohibited this crunch_company from being saved:</h2>

      <ul>
      <% @crunch_company.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :company_name %><br />
    <%= f.text_field :company_name %>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
